# 极简模式收尾：清理重构残留 + 仓库同步

> 状态：**待用户确认**（按项目纪律，文档未过不写代码）
> 日期：2026-09-19
> 前置：`6ed6596 refactor(core): 极简模式——砍掉 Web GUI/RAG/技能/MCP/子Agent/Trace 等附加层`

---

## 0. 一句话背景

2026-08-26 的 commit `6ed6596` 做了「极简模式」重构，**删除 76 个文件 / 19,279 行**，砍掉
Web GUI、RAG 记忆、技能、MCP、review、context、history、llm 八个模块。

**但这次重构只删了 `src/` 下的实现，没有清理依赖它的测试、评测与文档。**
结果是：`test/` 32 个文件、`eval/` 3 个文件、`README.md` / `AGENTS.md` 全部指向已不存在的模块。

用户决策（2026-09-19）：**记忆层不要、MCP 不要、技能不要，全部删除；仓库同步修改。**

---

## 1. 现状盘点（证据驱动）

### 1.1 被删模块清单（来源：`git show --stat 6ed6596`）

| 模块 | 原路径 | 规模 | 删除 commit |
|---|---|---|---|
| Web GUI 后端 | `src/gui/` | 8 文件 | `6ed6596` |
| Web GUI 前端 | `src/gui/web/` | 22 文件 | `6ed6596` |
| 记忆层 RAG | `src/memory/` | 18 文件 | `6ed6596` |
| 技能系统 | `src/skills/` | 2 文件 | `6ed6596` |
| MCP 客户端 | `src/mcp/` | 2 文件 | `6ed6596` |
| 审查/反思 | `src/review/` | 4 文件 | `6ed6596` |
| 上下文/历史 | `src/context/` | 2+ 文件 | `6ed6596` |
| 会话历史面板 | `src/history/` | 2 文件 | `6ed6596` |
| DeepSeek API 封装 | `src/llm/` | 2 文件 | `6ed6596` |

### 1.2 磁盘实测（`src/` 现存 28 文件，7 个目录）

```
src/agent/       loop.ts  output-style.ts  pi-agent.ts  pi-tools.ts  system-prompt.ts
src/app/         assemble.ts  chat.ts  keyContext.ts  markdown-lines.ts  types.ts
                 useAgentController.ts  viewport.ts
src/auth/        credentials.ts
src/cli/         app.tsx  auth.ts  login.tsx  main.ts  Markdown.tsx  sanitize.ts
                 thinkingIndicator.tsx  whaleArt.ts
src/config/      model-mode.ts  workspace.ts
src/permission/  index.ts  permission-system.ts
src/utils/       logger.ts  markdown.ts  rollback.ts
```

**28 个文件全部有引用，零孤儿。** 源码本身是干净的。

### 1.3 残留物实测（三大类）

> **统计方法说明（重要）**：本节数据由「逐个文件 `Test-Path` 判定」得出，
> 即对 `test/`、`eval/` 中每个 TS 文件，提取其所有 `../src/...` import，
> 逐个检查目标文件在磁盘上是否存在。这是确定性方法，不会因正则差异产生偏差。
>
> 初版文档曾用「按模块名 grep」的近似方法，得出 32 个损坏、22 个可用 —— **低估**。
> 精确值为 **47 个损坏、15 个可用**。差异来源：初版遗漏了 `src/tools/`（19 次引用）
> 及 `src/agent/core.ts`、`src/agent/middleware/`、`src/agent/model-mode.ts`、
> `src/agent/subagent.ts`、`src/app/commands.ts`、`src/auth/accounts.ts`、`src/evil.ts`。

#### A. 坏测试/评测：47 个（`test/` 44 个 + `eval/` 3 个）

**实测运行结果**：抽测 21 个"疑似可用"文件，**pass=13 / fail=8**，
8 个失败均为 `ERR_MODULE_NOT_FOUND`（真实失败，非脚本误报）。

运行时证据（`npx tsx test/embedder.test.ts`）：
```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
'.../src/memory/embedder.ts' imported from '.../test/embedder.test.ts'
```

按被删模块归类（引用次数来源：全量扫描 `test/` + `eval/`）：

| 被删模块 | 被引用次数 | 说明 |
|---|---|---|
| `src/memory/` | 43 | 记忆层 RAG（用户确认删除） |
| `src/tools/` | 19 | **工具实现（Agent 核心，规模最大）** |
| `src/context/` | 16 | 多轮历史 + JSONL trace |
| `src/gui/` | 11 | Web GUI 前后端 |
| `src/llm/` | 9 | DeepSeek API 封装 |
| `src/review/` | 7 | 审查 / 反思编排 |
| `src/skills/` | 1 | 技能加载器 |
| `src/agent/core.ts` 等 | — | `core.ts`、`middleware/`、`model-mode.ts`、`subagent.ts` |
| `src/app/commands.ts` | — | 命令注册 |
| `src/auth/accounts.ts` | — | 账户体系（GUI 用） |
| `src/evil.ts` | 1 | 测试用的恶意路径样例文件 |

**完整损坏清单（47 个）：**

```
test/（44 个）
  accounts-concurrency.test.ts      adversarial-context.ts
  adversarial-session.ts            adversarial-stops.ts
  auth-storage.test.ts              backend.test.ts
  bg-trash-sweep.test.ts            chatarea-render.test.tsx
  core.test.ts                      cross-proc-lock.test.ts
  embedder.test.ts                  extractor.test.ts
  format-validator.test.ts          gatekeeper.test.ts
  guardrails.test.ts                gui-memory.test.ts
  idle-revise.test.ts               json-schema-strict.test.ts
  loop-assistant-persist.test.ts    memory-intent.test.ts
  metrics.test.ts                   model-mode.test.ts
  orchestrator.test.ts              pipeline.test.ts
  reflection.test.ts                replay-all-remap.test.ts
  retriever.test.ts                 review-orchestrator.test.ts
  scrollFollow.test.ts              security.test.ts
  skills-loader.test.ts             thinking-layout.test.ts
  thinking-persist.test.ts          thinkingCard-render.test.tsx
  thinkingLayout.test.ts            thread-store-cascade.test.ts
  tools-impl.test.ts                trace-flush.test.ts
  trace-logger-flush.test.ts        trace-replay-all.test.ts
  validation.test.ts                vector-retriever.test.ts
  workspace.test.ts                 wrappers.test.ts

eval/（3 个）
  run-eval.ts                       review-audit-test.ts
  smoke-test.ts
```

**完好文件（15 个，即清理后应全部通过）：**

```
test/（12 个）
  eval-cases.test.ts        final-first-chunk.test.ts   flushSync-condition.test.ts
  logger.test.ts            markdown-lines.test.ts      optimistic-dedup.test.ts
  permission-system.test.ts sanitize.test.ts             task-history-atom.test.ts
  thinkings-by-task.test.ts ui-char-width.test.ts        useTypewriter-init.test.ts

非测试（3 个，保留）
  test/memory-fixtures.ts   eval/cases.ts               eval/types.ts
```

#### B. 坏评测：3 个（`eval/` 下）

| 文件 | 坏 import |
|---|---|
| `eval/run-eval.ts` | `src/llm/deepseek.ts`、`src/context/history.ts`、`src/agent/subagent.ts`、`src/tools/index.ts` |
| `eval/review-audit-test.ts` | `src/llm/deepseek.ts`、`src/context/history.ts` |
| `eval/smoke-test.ts` | `src/llm/deepseek.ts`、`src/context/history.ts` |

**你的 23 用例评测套件目前跑不起来。**

#### C. 死依赖：10 个（`package.json` 声明但源码零引用）

| 依赖 | 归属 | 验证方式 |
|---|---|---|
| `@modelcontextprotocol/sdk` | MCP（用户确认删） | 仅出现在 package.json / lock |
| `@huggingface/transformers` | 记忆层 embedding | 仅出现在 package.json / lock / docs |
| `react-markdown` | Web GUI | 仅 package.json / lock / 1 条测试注释 |
| `remark-breaks` | Web GUI | 仅 package.json / lock |
| `remark-gfm` | Web GUI | 仅 package.json / lock |
| `gsap` | Web GUI 动画 | 仅 package.json / lock |
| `@gsap/react` | Web GUI 动画 | 仅 package.json / lock |
| `lucide-react` | Web GUI 图标 | 仅 package.json / lock |
| `three` | Web GUI 3D | 仅 package.json / lock |
| `@types/three` | 同上（dev） | 仅 package.json / lock |

验证命令：
```bash
# 对每个包执行，确认 src/ 零命中
grep -rn "包名" src/
```

#### D. 文档失配

| 文件 | 问题 |
|---|---|
| `README.md:177-198` | 项目结构列出 `gui/ gui/web/ llm/ tools/ context/ history/ memory/ review/ skills/ mcp/` —— **11 个目录中 9 个已不存在** |
| `README.md:200-209` | 开发章节的 `npm test`、`npm run eval:check`、`npm run check:mcp`、`npm run check:providers` —— **package.json 里均不存在**（现仅 `start` / `dev` / `typecheck`） |
| `README.md:171-175` | 「技能系统」整节描述已删功能 |
| `README.md:214-219` | 安全说明含 Web 账户 / MCP 相关描述 |
| `AGENTS.md` | 第 3 节工具表、第 7 节差异化特性（含 `review_code`、`audit_dependencies`、`terminology`、`project_discover`、`delegate`、MCP、记忆层）—— 全部已删 |
| `docs/` 9 篇 | 描述已删功能的方案/报告（详见 §5） |

#### E. CI 失效

`.github/workflows/ci.yml:37-38` 执行 `npm test`，但该脚本已在 `6ed6596` 中从 package.json 移除。
**CI 每次推送都会失败**（第 37 行找不到脚本）。

#### F. `tsconfig.json` 检查盲区

```json
"include": ["src"]     // ← 不包含 test/ 和 eval/
```

后果：`npm run typecheck` 报 0 错误，**但 32 个测试文件与 3 个评测文件的坏引用完全检查不到**。
这就是「typecheck 通过却测试全崩」的机制性原因。

---

## 2. 能力边界与诚实声明

**本次清理能做到：**
- 让 `npm test` 从「44 崩」变为「12 全过」
- 让依赖树与源码真实需求一致
- 让 README / AGENTS.md 描述的架构与磁盘一致
- 让 CI 恢复绿色

**本次清理不能做到：**
- **不能恢复** memory / MCP / skills / gui / llm / context / review / **tools** 的功能——按用户决策这些是**永久删除**，代码只存在于 git 历史（`6ed6596` 之前）
- 不能保证 12 个「完好」测试全部通过（未逐个实际运行，仅用 `Test-Path` 验证 import 目标存在）
- 不能补充新测试覆盖被删模块留下的功能空白
- **不能消除 `src/tools/` 缺失带来的功能空洞**——工具是 Agent 核心，当前实现位于
  `src/agent/pi-tools.ts`（4 个原子工具 read/write/edit/bash），原 `src/tools/` 下的
  `review.ts`、`audit.ts`、`code-verify.ts`、`verify-answer.ts`、`verify-task.ts`、
  `discovery.ts`、`gatekeeper.ts`、`validation.ts`、`orchestrator.ts` 均已删除

**风险如实披露：**
- 删除 47 个测试/评测文件后，**测试覆盖率大幅下降**（`test/` 从 56 → 12 个，-79%）
- `eval/run-eval.ts` 删除后，**23 用例评测套件实际失效**（除非后续重写适配新架构）
- 若日后想恢复任一模块，需从 `6ed6596^` 取回，且与新代码存在兼容成本

---

## 3. 需求分层

### P0（必须做，否则仓库处于破损状态）

| # | 动作 | 影响文件数 |
|---|---|---|
| P0-1 | 删除 44 个坏测试 + 3 个坏评测 | 47 |
| P0-2 | 删除 15 个磁盘垃圾 | 15（未追踪，不走 git） |
| P0-3 | 移除 10 个死依赖 | 1（package.json）+ lock |
| P0-4 | 修 CI（移除 `npm test` 或补回脚本） | 1 |
| P0-5 | 重写 README 架构章节 | 1 |
| P0-6 | 重写 AGENTS.md 工具/特性章节（含 `src/tools/` 已删的说明） | 1 |

### P1（建议做）

| # | 动作 | 说明 |
|---|---|---|
| P1-1 | `tsconfig.json` 纳入 `test/` + `eval/` | 消除检查盲区，防止同类问题复发 |
| P1-2 | `docs/` 过期文档归档到 `docs/archive/` | 保留追溯价值，不污染主文档区 |
| P1-3 | 补回 `npm test` 脚本 | 用 `node --test` 跑剩余 12 个测试 |

### P2（可选）

| # | 动作 |
|---|---|
| P2-1 | 重写评测套件适配新架构（原 23 用例依赖已删的 `llm/`、`context/`、`tools/`） |
| P2-2 | 为现有 28 个源文件补测试 |

---

## 4. 改动文件清单

### 4.1 删除（62 个文件）

**坏测试 44 个**（`test/`）— 清单见 §1.3

**坏评测 3 个**（`eval/`）
```
run-eval.ts   review-audit-test.ts   smoke-test.ts
```

**磁盘垃圾 15 个**（未被 git 追踪）
```
vite.config.ts.timestamp-*.mjs   × 12   （Vite 运行残留，31,740 bytes）
.workbuddy/web-build{,2,3}.log   × 3    （内容完全相同，6,966 bytes）
```

### 4.2 修改（3 个文件）

| 文件 | 改动 | 原因 | 影响范围 |
|---|---|---|---|
| `package.json` | 删 10 个依赖；补 `test` 脚本 | 依赖与源码对齐 | 需重跑 `npm install` |
| `README.md` | 重写「项目结构」「开发」「技能系统」「安全说明」4 节 | 9/11 目录描述失效 | 使用者文档 |
| `AGENTS.md` | 重写「工具列表」「差异化特性」「架构边界」3 节 | 描述已删功能 | Agent 操作手册 |
| `.github/workflows/ci.yml` | 修正测试步骤 | CI 当前必失败 | CI 流水线 |
| `tsconfig.json` | `include` 加 `test`、`eval`（P1） | 消除检查盲区 | typecheck 范围 |
| `eval/RESULTS.md` | 标注评测套件已失效 | 避免误导 | 文档 |

### 4.3 归档（9 篇 docs）

移入 `docs/archive/`：
```
memory-design-survey.md              记忆层调研（功能已删）
记忆系统-轻量RAG-技术报告.md          记忆层技术报告（功能已删）
anti-loop-comparison.md              防循环对比（对应 loop-guard）
loop-guard-redesign.md               防循环重设计（对应已删逻辑）
pro-cache-optimization.md            缓存优化（对应已删 llm 层）
structured-output-plan.md            结构化输出方案（对应已删 structured-parse）
gui-integration-plan.md              Web GUI 集成方案（功能已删）
realtime-rendering-design.md         实时渲染设计（对应已删 gui）
architecture-improvement-plan.md     架构改进方案（部分已过时）
```

**保留在原位（7 篇，有持续价值）：**
```
docs/Bug修复-*.md              × 6   真实工程记录，含实证
docs/框架选型决策.md                  技术选型依据
docs/commands.md                      命令参考（需核对）
docs/TUI界面优化-需求与方案.md        当前 TUI 需求
docs/UX优化-*.md               × 2   当前 UX 方案
docs/学习路线.md                      学习路径
docs/THINKING_BOX_TROUBLESHOOTING.md  排查手册（需核对）
docs/permissions-matrix.svg           权限矩阵图（需核对）
```

### 4.4 待用户决策（未列入本次执行）

| 对象 | 问题 |
|---|---|
| `docs/面试讲解.md` | 内容描述已删功能（记忆层、MCP）—— 重写 / 归档 / 删除？ |
| `docs/GAP_VS_MINICODE.md` | 同上（对比表中含记忆层） |
| `test/tools-impl.test.ts` | 当前测 4 个原子工具，是否扩展覆盖 `pi-tools.ts`？ |
| `eval/cases.ts` + `golden-cases.md` | 23 用例定义，是否保留作为未来重写的素材？ |
| `scripts/spike-*.mjs`（9 个） | 一次性排查脚本，保留 / 归档 / 删除？ |
| `.git` 里 18 个 tag | 是否保留 `mem-M0~M10`（记忆层里程碑，功能已删）？ |

---

## 5. 执行顺序（分批 commit）

| 批次 | 内容 | commit message 建议 | 验证 |
|---|---|---|---|
| 1 | 删 15 个磁盘垃圾 | `chore(repo): 清理 Vite 残留与重复构建日志` | 目录干净 |
| 2 | 删 44 坏测试 + 3 坏评测 | `refactor(test): 移除引用已删模块的失效测试与评测` | 12 个测试全过 |
| 3 | 移除 10 个死依赖 | `chore(deps): 卸载极简模式后无引用的依赖` | `npm install` + typecheck |
| 4 | 修 CI + tsconfig + 补 test 脚本 | `ci: 修正测试步骤并将 test/eval 纳入类型检查` | `npm test` |
| 5 | 重写 README + AGENTS.md | `docs: 使文档与极简模式后的实际架构一致` | 人工核对 |
| 6 | docs 归档 | `docs: 归档描述已删功能的方案与报告` | 路径检查 |

**每批独立 commit，逐批验证，不合并。**

---

## 6. 验收标准

| 编号 | 标准 | 验证命令 | 期望 |
|---|---|---|---|
| V1 | 无坏引用 | 逐个文件 `Test-Path` 检查 `../src/...` import 目标 | 0 个缺失 |
| V2 | 类型检查通过 | `npx tsc --noEmit` | exit 0 |
| V3 | 测试可运行 | 逐个 `npx tsx test/xxx.test.ts` | 12/12 passed |
| V4 | 无死依赖 | 逐包 `grep -rn "包名" src/` | 0 命中 |
| V5 | 文档与磁盘一致 | 对照 README 项目结构 vs `ls src/` | 完全一致 |
| V6 | 目录干净 | `ls` 根目录 | 无 timestamp 文件 |
| V7 | CI 配置有效 | 推送后查看 Actions | 绿色 |

---

## 7. 风险与决策点

| 风险 | 等级 | 缓解 |
|---|---|---|
| 误删仍有价值的测试 | 🟡 中 | 全部删除前先按 §1.3 清单核对；git 历史可恢复 |
| 卸载依赖导致构建失败 | 🟡 中 | 卸载后立即 `npm install` + typecheck + 冒烟启动 |
| README 重写引入新错误 | 🟢 低 | 重写前先 `ls src/` 取真实结构 |
| 覆盖率下降 79% | 🔴 高 | **用户已知悉**；P2 建议后续补测试 |
| 评测套件失效 | 🔴 高 | **用户已知悉**；`cases.ts` 保留作素材 |
| **`src/tools/` 功能空洞** | 🔴 高 | 原 9 个工具文件（review/audit/code-verify/verify-*/discovery/gatekeeper/validation/orchestrator）已删，现仅 `agent/pi-tools.ts` 4 个原子工具。若需差异化工具，须重新实现 |

**决策点：**
1. 44 个坏测试——**删** 还是改造？（判据：改造需先恢复被删模块，与用户决策矛盾 → 建议删）
2. 3 个坏评测——同上（`cases.ts` 用例定义建议保留）
3. `docs/` 9 篇过期文档——归档 vs 删除？（建议归档，保留追溯）
4. **`src/tools/` 是否需要在极简模式下重新实现差异化工具？**（如 `review_code`、`audit_dependencies` 等曾是 AGENTS.md 宣称的核心竞争力，现均已随模块删除）

---

## 8. 后续接入指引（若日后恢复某模块）

从 `6ed6596^` 取回单模块：
```bash
# 查看删除前状态
git show 6ed6596^:src/memory/manager.ts

# 恢复整个目录
git checkout 6ed6596^ -- src/memory/
```

**注意**：恢复后需手动适配，因为：
- `pi-agent.ts` 在 `6ed6596` 中被改过（去掉了记忆注入）
- `loop.ts` 去掉了 `MemoryService` 引用
- `chat.ts` 去掉了 `/memory` 命令
- 依赖需重新安装（`@huggingface/transformers`）

---

## 9. 附：本次清理的依据 commit

| commit | 说明 |
|---|---|
| `6ed6596` | 极简模式重构主体（-19,279 行） |
| `01262b6` | 补全 Pi Agent 依赖 |
| `2bf2bfe` | 规范 earendil 版本号 |
| `d85f039` | 本次会话前置 commit（.gitignore + 散落文件入库） |
